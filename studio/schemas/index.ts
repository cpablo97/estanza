import { localeString, localeText, localeBlock, localeImage } from "./locale";
import { heroSection, textImageSection, servicesSection, stepsSection, contactSection, closingSection, genericSection } from "./sections";
import { landing, settings } from "./documents";

export const schemaTypes = [
  localeString, localeText, localeBlock, localeImage,
  heroSection, textImageSection, servicesSection, stepsSection, contactSection, closingSection, genericSection,
  landing, settings,
];
